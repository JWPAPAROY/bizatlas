node_modules
dist
.env
.env.local
.DS_Store
*.log
.vite

# gh 토큰에 workflow 스코프가 없어 워크플로 파일은 푸시가 거부된다.
# `gh auth refresh -h github.com -s workflow` 실행 후 이 두 줄을 지우고 커밋하면
# push-to-deploy 가 활성화된다.
.github/workflows/
